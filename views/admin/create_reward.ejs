<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Add Reward - Admin</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <%- include('../partials/nav') %>

  <main class="container admin-container">
    <%- include('../partials/messages') %>

    <h1>➕ Add New Reward</h1>

    <div class="admin-form-card">
      <form method="POST" action="/admin/rewards/create" class="admin-form">
        <div class="form-group">
          <label for="title">Reward Title *</label>
          <input type="text" id="title" name="title" required placeholder="e.g., Extra Screen Time" class="mobile-input">
        </div>

        <div class="form-group">
          <label for="description">Description</label>
          <textarea id="description" name="description" rows="3" placeholder="Details about this reward" class="mobile-input"></textarea>
        </div>

        <div class="form-group">
          <label for="cost_points">Points Cost *</label>
          <input type="number" id="cost_points" name="cost_points" required value="100" min="1" max="10000" class="mobile-input">
        </div>

        <div class="form-group">
          <label class="checkbox-label">
            <input type="checkbox" id="active" name="active" value="1" checked>
            <span>Active (available to redeem)</span>
          </label>
        </div>

        <div class="form-actions mobile-actions">
          <button type="submit" class="btn btn-primary mobile-btn">Create Reward</button>
          <a href="/admin/rewards" class="btn btn-secondary mobile-btn">Cancel</a>
        </div>
      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
