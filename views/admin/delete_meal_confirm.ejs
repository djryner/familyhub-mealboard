<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Delete Meal - Admin</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <%- include('../partials/nav') %>

  <main class="container admin-container">
    <%- include('../partials/messages') %>

    <div class="admin-form-card">
      <h1>⚠️ Delete Meal</h1>
      
      <div style="margin: 2rem 0;">
        <p style="font-size: 1.1rem; margin-bottom: 1rem;">Are you sure you want to delete this meal?</p>
        
        <div style="background: #f8f9fa; padding: 1.5rem; border-radius: 8px; margin-bottom: 2rem;">
          <div style="font-weight: bold; font-size: 1.2rem; margin-bottom: 0.5rem;">
            <%= meal.title %>
          </div>
          <div style="color: #666; margin-bottom: 0.5rem;">
            📅 <%= meal.date %>
            <% if (meal.meal_type) { %>
              • <%= meal.meal_type.charAt(0).toUpperCase() + meal.meal_type.slice(1) %>
            <% } %>
          </div>
          <% if (meal.description) { %>
            <div style="color: #555; margin-top: 0.5rem;">
              <%= meal.description %>
            </div>
          <% } %>
        </div>

        <p style="color: #dc3545; font-weight: 600;">This action cannot be undone.</p>
      </div>

      <div class="form-actions mobile-actions">
        <form method="POST" action="/admin/meals/<%= meal.id %>/delete" style="display: inline;">
          <button type="submit" class="btn btn-danger mobile-btn">✅ Yes, Delete This Meal</button>
        </form>
        <a href="/admin/meals" class="btn btn-secondary mobile-btn">❌ Cancel</a>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
