<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Delete User - Admin</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <%- include('../partials/nav') %>

  <main class="container admin-container">
    <%- include('../partials/messages') %>

    <div class="admin-form-card">
      <h1>⚠️ Delete User</h1>
      
      <div style="margin: 2rem 0;">
        <p style="font-size: 1.1rem; margin-bottom: 1rem;">Are you sure you want to delete this user?</p>
        
        <div style="background: #f8f9fa; padding: 1.5rem; border-radius: 8px; margin-bottom: 2rem;">
          <div style="font-weight: bold; font-size: 1.2rem; margin-bottom: 0.5rem;">
            <%= user.name %>
          </div>
          <div style="color: #666;">
            ⭐ <%= user.balance || 0 %> points
            <% if (user.is_parent) { %>
              • 👑 Parent
            <% } %>
          </div>
        </div>

        <p style="color: #dc3545; font-weight: 600;">This will permanently delete the user and all their points history.</p>
      </div>

      <div class="form-actions mobile-actions">
        <form method="POST" action="/admin/users/<%= user.id %>/delete" style="display: inline;">
          <button type="submit" class="btn btn-danger mobile-btn">✅ Yes, Delete This User</button>
        </form>
        <a href="/admin/users" class="btn btn-secondary mobile-btn">❌ Cancel</a>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
