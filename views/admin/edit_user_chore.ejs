<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Edit Chore - <%= chore.title %></title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <%- include('../partials/nav') %>

  <main class="container admin-container">
    <%- include('../partials/messages') %>

    <h1>✏️ Edit Chore</h1>

    <div class="admin-form-card">
      <form method="POST" action="/admin/chores/<%= chore.task_id %>/edit" class="admin-form">
        <div class="form-group">
          <label for="title">Chore Name *</label>
          <input type="text" id="title" name="title" value="<%= chore.title %>" required placeholder="e.g., Clean Room" class="mobile-input">
        </div>

        <div class="form-group">
          <label for="frequency">Frequency *</label>
          <select id="frequency" name="frequency" required class="mobile-input">
            <option value="">-- Select Frequency --</option>
            <option value="daily" <%= chore.recurrence === 'daily' ? 'selected' : '' %>>Every Day</option>
            <option value="weekends" <%= chore.recurrence === 'weekends' ? 'selected' : '' %>>Weekends (Sat-Sun)</option>
            <option value="weekdays" <%= chore.recurrence === 'weekdays' ? 'selected' : '' %>>Weekdays (Mon-Fri)</option>
            <option value="school-week" <%= chore.recurrence === 'school-week' ? 'selected' : '' %>>School Week (Sun-Thu)</option>
          </select>
          <small class="form-text">How often should this chore be completed?</small>
        </div>

        <div class="form-group">
          <label for="points">Points per Completion *</label>
          <input type="number" id="points" name="points" value="<%= chore.points %>" min="1" required class="mobile-input">
          <small class="form-text">How many points should be awarded for completing this chore?</small>
        </div>

        <div class="alert alert-info">
          <strong>Note:</strong> Changing the frequency will regenerate future occurrences of this chore. Past completions will remain unchanged.
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary mobile-btn">💾 Save Changes</button>
          <% if (user) { %>
            <a href="/admin/chores/user/<%= user.id %>" class="btn btn-secondary mobile-btn">Cancel</a>
          <% } else { %>
            <a href="/admin/chores" class="btn btn-secondary mobile-btn">Cancel</a>
          <% } %>
        </div>
      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
