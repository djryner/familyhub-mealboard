<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>Edit Reward - Admin</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="admin-body">
  <%- include('../partials/nav') %>

  <main class="container admin-container">
    <%- include('../partials/messages') %>

    <div class="admin-header">
      <h1>✏️ Edit Reward</h1>
    </div>

    <div class="card">
      <form method="POST" action="/admin/rewards/<%= reward.id %>/edit" class="admin-form">
        <div class="form-group">
          <label for="title">Reward Title *</label>
          <input 
            type="text" 
            id="title" 
            name="title" 
            class="form-control" 
            value="<%= reward.title %>"
            placeholder="e.g., Extra Screen Time"
            required
          >
        </div>

        <div class="form-group">
          <label for="emoji">Emoji (optional)</label>
          <input 
            type="text" 
            id="emoji" 
            name="emoji" 
            class="form-control" 
            value="<%= reward.emoji || '' %>"
            placeholder="e.g., 🎁"
            maxlength="2"
          >
          <small style="color: #666;">Single emoji to display with the reward</small>
        </div>

        <div class="form-group">
          <label for="cost_points">Points Cost *</label>
          <input 
            type="number" 
            id="cost_points" 
            name="cost_points" 
            class="form-control" 
            value="<%= reward.cost_points %>"
            min="1" 
            max="10000"
            required
          >
        </div>

        <div class="form-group">
          <label class="checkbox-label">
            <input 
              type="checkbox" 
              id="active" 
              name="active" 
              <%= reward.active ? 'checked' : '' %>
            >
            <span>Active (available to redeem)</span>
          </label>
        </div>

        <div class="form-actions">
          <button type="submit" class="btn btn-primary mobile-btn">💾 Save Changes</button>
          <a href="/admin/rewards" class="btn btn-secondary mobile-btn">Cancel</a>
        </div>
      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
</body>
</html>
